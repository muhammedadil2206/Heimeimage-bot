{
  "name": "heimage-bot",
  "version": "1.0.0",
  "description": "Full-stack text-to-image generator using Clipdrop API",
  "private": true,
  "scripts": {
    "start": "concurrently -k -c \"cyan,magenta\" -n \"SERVER,CLIENT\" --kill-others-on-fail \"npm run dev:server\" \"npm run dev:client\"",
    "dev": "npm run start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:win": "powershell -Command \"Start-Process powershell -ArgumentList '-NoExit', '-Command', 'cd server; npm run dev'; Start-Sleep -Seconds 2; Start-Process powershell -ArgumentList '-NoExit', '-Command', 'cd client; npm run dev'\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "build:server": "cd server && npm install"
  },
  "keywords": [
    "text-to-image",
    "clipdrop",
    "mern",
    "react",
    "express",
    "mongodb"
  ],
  "author": "Muhammed Adil",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
